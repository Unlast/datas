//INSTALAR Angular

npm install -g npm@latest       : Actualizar npm

npm cache clean --force         : Borrar cache

npm set audit false             : desactivar las auditorias de npm para evitar fallos

npm uninstall -g angular-cli    : Desinstalar los paquetes anteriores de Angular cli
npm uninstall -g @angular/cli   :

npm cache clean --force 

npm install -g @angular/cli@latest  : Instalar la ultima version de angular

Primero nos pedirá el nombre del proyecto
Nos preguntará si queremos añadir el routing de angular, diremos que NO
Nos preguntará si queremos usar un formato especifico para los estilos css, le daremos a enter simplemente
Esperamos a que el asistente acabe de generar nuestro proyecto de Angular 7

cd NOMBRE_DEL_PROYECTO
ng serve
//(http://localhost:4200/)


//INSTALAR REACT

npm install -g create-react-app

cd

create-react-app NombredelProyecto

cd NombreDelProyecto

npm start
http://localhost:3000/

//INSTALAR VUE

npm install -g vue-cli
vue init webpack-simple nombre-del-proyecto
cd nombre-del-proyecto
npm install
npm run dev

//Verficar version vue
vue --version                 : version de vue
npm i -g vue                  :update vue
npm i -g vue-cli              :update vue-cli

//JENKINS
//iniciar a traves de la cmd
cd ubicacion-de-jenkins
java -jar jenkins.war  

//Cambiar puerto de jenkins
java -jar jenkins.war --httpPort=9090
java -jar jenkins.war --httpsPort=9090
